namespace AbstractFactory.Users
{
    public class StudentClient
    {
        // 1.Add a check if the client registration credential are valid for a StudentClientAccount
        // 2.Extend/diversify the abstractions
    }
}